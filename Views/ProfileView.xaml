<Page x:Class="PharmaDesk.Views.ProfileView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource BackgroundBrush}">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource HeaderBar}">
            <TextBlock Text="My Profile" Style="{StaticResource PageTitle}" VerticalAlignment="Center"/>
        </Border>

        <ScrollViewer Margin="24">
            <StackPanel MaxWidth="640" HorizontalAlignment="Center">

                <!-- Avatar + name -->
                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,24">
                    <Border Width="80" Height="80" CornerRadius="40"
                            Background="#FF14213D" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding Initials}" FontSize="28" FontWeight="Bold"
                                   Foreground="#FFFCA311"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="{Binding FullName}" FontSize="18" FontWeight="SemiBold" Margin="0,12,0,0"
                               Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Email}" FontSize="13" Margin="0,4,0,0"
                               Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Stats row -->
                <Border Style="{StaticResource BentoCardStyle}" Margin="0,0,0,24">
                    <UniformGrid Columns="3">
                        <!-- Total Orders -->
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="{Binding TotalOrders}" Style="{StaticResource KpiValue}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Total Orders" Style="{StaticResource CardLabel}"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                        <!-- Total Spent -->
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Style="{StaticResource KpiValue}" HorizontalAlignment="Center">
                                <Run Text="lei "/><Run Text="{Binding TotalSpent, StringFormat=N0}"/>
                            </TextBlock>
                            <TextBlock Text="Total Spent" Style="{StaticResource CardLabel}"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                        <!-- Member Since -->
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="{Binding MemberSince}" Style="{StaticResource KpiValue}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Member Since" Style="{StaticResource CardLabel}"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </UniformGrid>
                </Border>

                <!-- Edit form -->
                <Border Style="{StaticResource BentoCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Edit Profile" Style="{StaticResource SectionHeading}" Margin="0,0,0,16"/>

                        <!-- Full Name -->
                        <TextBlock Text="FULL NAME" Style="{StaticResource CardLabel}" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}"
                                 Height="44" Padding="12,0"
                                 VerticalContentAlignment="Center"
                                 Background="{DynamicResource SurfaceBrush}"
                                 Foreground="{DynamicResource TextPrimaryBrush}"
                                 Margin="0,0,0,16">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                        <!-- Email -->
                        <TextBlock Text="EMAIL" Style="{StaticResource CardLabel}" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                                 Height="44" Padding="12,0"
                                 VerticalContentAlignment="Center"
                                 Background="{DynamicResource SurfaceBrush}"
                                 Foreground="{DynamicResource TextPrimaryBrush}"
                                 Margin="0,0,0,16">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                        <!-- Save button -->
                        <Button Content="Save Changes" Style="{StaticResource PrimaryButton}"
                                Command="{Binding SaveCommand}" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Page>
