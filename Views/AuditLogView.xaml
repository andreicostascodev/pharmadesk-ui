<Page x:Class="PharmaDesk.Views.AuditLogView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource BackgroundBrush}">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource HeaderBar}">
            <TextBlock Text="Audit Log" Style="{StaticResource PageTitle}" VerticalAlignment="Center"/>
        </Border>

        <DockPanel Margin="24">
            <!-- Filter bar -->
            <Border DockPanel.Dock="Top" Style="{StaticResource BentoCardStyle}" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal">
                    <!-- Date from -->
                    <TextBlock Text="FROM" Style="{StaticResource CardLabel}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker SelectedDate="{Binding DateFrom}" Width="140" Margin="0,0,16,0"
                                Background="{DynamicResource SurfaceBrush}"
                                Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <!-- Date to -->
                    <TextBlock Text="TO" Style="{StaticResource CardLabel}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker SelectedDate="{Binding DateTo}" Width="140" Margin="0,0,16,0"
                                Background="{DynamicResource SurfaceBrush}"
                                Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <!-- Action type filter -->
                    <TextBlock Text="ACTION" Style="{StaticResource CardLabel}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding ActionTypes}"
                              SelectedItem="{Binding SelectedActionType}"
                              Width="140" Margin="0,0,16,0"
                              Background="{DynamicResource SurfaceBrush}"
                              Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <Button Content="Apply" Style="{StaticResource PrimaryButton}" Height="36"
                            Command="{Binding ApplyFilterCommand}"/>
                </StackPanel>
            </Border>

            <!-- Audit log DataGrid -->
            <Border Style="{StaticResource BentoCardStyle}" Padding="0">
                <DataGrid ItemsSource="{Binding LogEntries}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          RowBackground="{DynamicResource BackgroundBrush}"
                          AlternatingRowBackground="{DynamicResource SurfaceBrush}"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="{DynamicResource ThemeBorderBrush}"
                          ColumnHeaderHeight="40"
                          RowHeight="48">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="12,0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="12,0"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <!-- Timestamp (monospace) -->
                        <DataGridTemplateColumn Header="Timestamp" Width="180" SortMemberPath="Timestamp" CanUserSort="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock FontFamily="Cascadia Code, Consolas" FontSize="13"
                                               Text="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- User -->
                        <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="140" CanUserSort="True"/>

                        <!-- Action type (colour-coded) -->
                        <DataGridTemplateColumn Header="Action" Width="140" SortMemberPath="Action" CanUserSort="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Action}" FontWeight="SemiBold" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Action, StringFormat={}{0}}" Value="Create">
                                                        <Setter Property="Foreground" Value="#FF22C55E"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action, StringFormat={}{0}}" Value="Delete">
                                                        <Setter Property="Foreground" Value="#FFEF4444"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action, StringFormat={}{0}}" Value="Update">
                                                        <Setter Property="Foreground" Value="#FFFCA311"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action, StringFormat={}{0}}" Value="Login">
                                                        <Setter Property="Foreground" Value="#FFA0AEC0"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Description -->
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" CanUserSort="False"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </DockPanel>
    </DockPanel>
</Page>
